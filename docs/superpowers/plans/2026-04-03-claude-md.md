# CLAUDE.md 最佳实践指南实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在项目根目录基于已确认的设计规格书创建 `CLAUDE.md` 文件，作为 AI 与开发者协同索引指南。

**Architecture:** 这是一个纯文档编写任务。提取 Spec 中的核心模块内容并组合为 `CLAUDE.md`，并在其中引用 `.claude/memory.md` 文件作为动态记忆存储源。

**Tech Stack:** Markdown

---

### Task 1: 生成并写入 CLAUDE.md 文件

**Files:**
- Create: `CLAUDE.md`

- [ ] **Step 1: 编写 CLAUDE.md 文件内容**

```bash
# 将之前设计的内容写入 CLAUDE.md，包含指引、架构上下文、开发铁律及调试流程
```

- [ ] **Step 2: 验证文件是否成功写入**

Run: `cat CLAUDE.md` (或使用相应的读取操作确认文件内容)
Expected: 输出完整的 CLAUDE.md 内容并检查是否正确关联了 `.claude/memory.md`

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: 新增 CLAUDE.md 规范与最佳实践设计"
```